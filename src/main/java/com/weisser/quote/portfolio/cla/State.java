package com.weisser.quote.portfolio.cla;

public enum State {
    /**
     * Variable is in the in set.
     */
    STATE_IN,

    /**
     * Variable is at upper limit.
     */
    STATE_UPPER,

    /**
     * Variable is at the lower limit.
     */
    STATE_LOWER
}
